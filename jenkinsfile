pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Add your build commands here
                echo "Build"
            }
        }
        stage('Test') {
            steps {
                // Add your test commands here
                echo "Test"
            }
        }
        stage('Deploy') {
            when {
                // Deploy only if the branch is master
                branch 'master'
            }
            steps {
                // Add your deployment commands here
                // For example, copy the built artifact to the deployment directory
                sh 'cp target/myapp.jar D:/Jenkins'
            }
        }
    }
}
